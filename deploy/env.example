# RouteScope Configuration Environment
# Place this file at /etc/routescope/env

# --- Server Configuration ---
# The HTTP port for the API server and Web Dashboard
RS_HTTP_PORT=8080

# The mode RouteScope runs in: "server", "agent" (future distributed mode)
RS_MODE=server

# --- Database ---
# Path to the SQLite database
RS_DB_PATH=/var/lib/routescope/routescope.db

# Retention period for historical data (days)
RS_DATA_RETENTION_DAYS=30

# --- Monitoring Targets (Comma separated) ---
# NOTE: These can also be configured via the Web UI (preferred)
RS_TARGETS=8.8.8.8,1.1.1.1

# --- SSH Speed Test Default Credentials (Optional) ---
# Only used if you want to statically configure a probe targets from env
# RS_SSH_USER=root
# RS_SSH_KEY_PATH=/etc/routescope/id_rsa
